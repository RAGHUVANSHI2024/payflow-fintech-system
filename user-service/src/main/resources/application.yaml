spring:
  application:
    name: user-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bank_user
    username: root
    password: mysql

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8082
